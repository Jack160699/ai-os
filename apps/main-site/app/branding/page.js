import { ServicePageClient } from "@/app/components/service/ServicePageClient";

export const metadata = {
  title: "Branding — Stratxcel",
  description: "Tight kits your team can reuse — type, colour, templates — so everything feels like one company.",
};

export default function BrandingPage() {
  return <ServicePageClient slug="branding" />;
}
