import { ServicePageClient } from "@/app/components/service/ServicePageClient";

export const metadata = {
  title: "Mobile apps — Stratxcel",
  description: "React Native apps for iOS and Android — scoped honestly, built in slices you can test.",
};

export default function MobileAppsPage() {
  return <ServicePageClient slug="mobile-apps" />;
}
