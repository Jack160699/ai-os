# Deprecated Admin Surface

`apps/ai-os` is no longer the primary admin frontend.

Primary admin app is now:

- `apps/stratxcel-os`

This app is retained temporarily for migration/backward compatibility only.
